import { ApiMethodConfig } from './Api.types';

/**
 * Базовый класс для API сервисов, предоставляющий готовые GET, POST, DELETE запросы
 */
export declare class ApiService {
    protected get<R>(config: ApiMethodConfig): Promise<R>;
    protected post<R>(config: ApiMethodConfig): Promise<R>;
    protected delete<R>(config: ApiMethodConfig): Promise<R>;
    private makeRequest;
}
